# AGENTS.md

## 项目目标

本项目用于构建可复现的 Ghidra Docker 环境，并支持主流、较新的 Linux
发行版。镜像应支持 Ghidra GUI 和无头分析场景，同时保持版本、
架构与构建参数清晰可追踪。

当前实现位于 `Dockerfile.ghidra`，通过 `BASE_IMAGE` 支持多个发行版，仅安装
Ghidra 及其 Java 运行依赖。

## 开发原则

- 保持镜像构建可复现。Ghidra、插件、基础镜像和关键依赖必须使用明确版本，
  不使用无约束的 `latest`。
- 新增发行版时复用一致的运行时接口；发行版差异仅应出现在基础镜像、
  包管理器和必要的兼容配置中。
- 优先支持仍处于官方维护期的稳定版发行版。目标范围包括 Debian、Ubuntu、
  Fedora、Rocky Linux、AlmaLinux、openSUSE 等主流发行版的较新版本。
- 不因新增发行版破坏现有构建参数、端口、挂载路径、启动命令或环境变量。
- 默认面向无头运行；如需 GUI，应通过独立镜像目标或明确的可选构建参数提供。
- 下载第三方发布物时优先校验 SHA-256；新增资源必须说明官方来源和版本兼容性。
- 国内镜像或代理必须可通过构建参数替换，不应成为唯一下载路径。
- 减少镜像层和体积，安装后清理包管理器缓存、临时文件和下载归档。
- 容器进程应正确传递信号并在子进程异常时退出；避免依赖脆弱的固定时长等待。
- 除非兼容性确有要求，运行时不使用 root；新增权限、端口和持久化目录需写入文档。

## 建议的仓库结构

随着发行版支持增加，优先采用以下结构：

```text
docker/
  debian/
  ubuntu/
  fedora/
  rocky/
scripts/
tests/
Dockerfile.ghidra
README.md
```

在真正出现共享逻辑或第二个发行版前，不要为了结构而提前拆分文件。

## 变更要求

- 修改构建逻辑前先阅读完整 Dockerfile 和相关脚本。
- 保持 `ARG` 命名一致，并在 README 中记录可覆盖参数及默认值。
- Shell 脚本使用 POSIX `sh`；只有确实依赖 Bash 功能时才使用 Bash。
- Dockerfile 中的远程下载应使用 `curl --fail` 或等效的失败检测。
- 不提交密钥、令牌、私有仓库凭据、带签名的临时 URL 或本地分析样本。
- 不静默修改用户已有的镜像源、代理配置或未提交改动。

## 验证

每次相关变更至少执行适用的检查：

1. 对所有受影响的发行版构建镜像。
2. 启动容器并确认 Ghidra 无头进程正常运行。
3. 检查声明的服务端口可访问，且健康检查能在限定时间内通过。
4. 验证 Ghidra 和 Java 的实际版本。
5. 使用最小测试二进制执行一次无头导入或分析。
6. 检查容器停止时无残留子进程，启动失败时返回非零状态。

若受网络、平台或时间限制无法完成完整构建，应明确记录已执行的检查和未验证项，
不得将静态检查表述为构建成功。

## 文档

README 应至少包含：

- 支持的发行版、版本、CPU 架构和维护状态矩阵；
- 构建、运行、端口、卷挂载和健康检查示例；
- Ghidra 与 JDK 的兼容版本；
- 下载镜像、代理及校验和的配置方式；
- 已知限制和升级流程。

提交新增发行版支持时，应同步更新支持矩阵和对应的构建验证结果。
